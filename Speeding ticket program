# Variables
int_user_money = int(input("Enter how much money you want to have. (after this, program will start)"))
max_speed = 51
int_user_speed = int(input("Do you know how fast you were going?"))

# Test line
print(int_user_speed)

# 
if int_user_speed < max_speed:
  print("Sorry for wasting your time.")
elif int_user_speed > max_speed:
  print("Thats over the speed limit, im going to have to fine $30 you for that")
